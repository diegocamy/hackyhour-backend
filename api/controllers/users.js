const JWT = require('jsonwebtoken');
require('dotenv').config();

module.exports = {
  googleOauth: async (req, res, next) => {
    try {
    } catch (error) {
      console.log(error);
    }
  },
};
